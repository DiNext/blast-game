{
  "name": "blast-game",
  "version": "0.1.0",
  "private": true,
  "description": "Blast puzzle game prototype — TypeScript + Cocos Creator 2.4.x",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:cov": "jest --coverage",
    "typecheck": "tsc -p tsconfig.jest.json --noEmit"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "^20.11.30",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2",
    "typescript": "^4.9.5"
  }
}
